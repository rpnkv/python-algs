from typing import List

class Solution:
    def pivotIndex(self, nums: List[int]) -> int:
        l, r = 0, len(nums) - 1
        l_sum = r_sum = 0

        while l < r:
            raise NotImplementedError

